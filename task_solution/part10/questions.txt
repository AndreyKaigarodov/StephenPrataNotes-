1. Что такое класс ?:
-Класс - пользовательский тип данных, который может содержать методы и поля для работы с данными
поля и методы могут быть приватными и публичными. К приватным полям и методам можно обратитсья только из
класса или friend функции

2. Каким образом класс обеспечивает абстракцию, инкапсуляцию и сокрытие данных ?:
-Абстракция достигается за счет выделения в сущности только необходимых параметров, которые
полно и цельно описывают систему. Инкуапсуляция и сокрытие достигается с помощью модификаторов доступа
и дружественных функций

3. Каково отношение между объектом и классом?
Объект - экземпляр класса, с ним можно работать с помощью методов и полей, описанных в классе.
сам объект может предоставлять только открытые интерфейсы для манипулирования даннымы.

4. Чем отличаются функции-члены класса от данных-членов класса помимо того, что они - функции?
функция-член - содержит указатель на объект ?
данные-члена класса

5.  Определите класс для представления банковского счета. Данные-члены должны включать имя вкладчика, номер счета
и баланс. Функции члены должны позволять следующее
- сощдание объекта и его инициализация
- отображение имени вкладчика, номера счета и баланса
- добавление на счет суммы денег, переданной в аргументе
- снятие суммы денег, переданной в аргументе
Привести только объявление без реализации методов
Файл: questions.h

6. а.Когда вызываются конструкторы класса? б.Когда вызываются конструкторы 
а) Констурктор класса вызывается при создании объекта т.е. при явном вызове консруктора, 
с помощью фуннкции new Clacc. При объявлении преременной с типом описанного класса вызывается конструктор
по умолчанию
б) Деструктор вызывается при выходе из области видимости или вызове для объекта функции delete

7. Написать код конструктора для класса банковского счета, описанного в вопросе 5
Решение: Файл  ХХХХХ
8. Что такое конструктор по умолчанию? Каковы выгоды его приенения?
- Не имеет параметров, либо все параметры заданы по умолчанию
Позволяет: объявлять объект без инициализации, позволяет объявлять массивы. 

9 Модифицируйте определение клаасса Stock так, чтобы он имел функции-члены, которые возвращают
значения индивидуальных данных-членов. Член, который возвращает наименование компании, не должен давать возможность
изменять массив. Т.е. он не может просто возвращать ссылку на string. Он может возвращать const-ссылку.
Результат:
class Stock
{
private:
    std::string company;
    int shares;
    double share_val;
    double total_val;
    void set_tot() { total_val = shares * share_val; }
public:
  //  Stock();        // default constructor
    Stock(const std::string & co, long n = 0, double pr = 0.0);
    ~Stock();       // do-nothing destructor
    void buy(long num, double price);
    void sell(long num, double price);
    void update(double price);
    void show()const;
    const Stock & topval(const Stock & s) const;
    //UPD:
    const string& getCompanyName() const {return company};
    double getTotalVal() const {return total_val};
    double getShareVal() const {return share_val};
    int getShares const {return shares};
};

10. Что такое this и *this
this - доступ к полям класса
*this - адресс объекта